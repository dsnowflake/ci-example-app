{
  "name": "ci-example-app",
  "version": "1.0.0",
  "description": "A simple app for a CI pipeline example.",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "lint": "eslint .",
    "prettier": "prettier --write .",
    "build": "echo 'Build successful!' && exit 0"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "jest": "^29.7.0",
    "prettier": "^3.2.5"
  }
}